{
  "name": "plumber",
  "version": "0.0.0",
  "author": "@kyo_ago",
  "browserify": {
    "transform": [
      "debowerify"
    ]
  },
  "devDependencies": {
    "bower": "^1.3.12",
    "browserify": "^8.1.1",
    "debowerify": "^1.2.0",
    "dtsm": "^0.1.0",
    "fixbow": "0.0.1",
    "fixpack": "^1.2.0",
    "gulp": "^3.8.10",
    "gulp-cli": "^0.1.4",
    "gulp-react": "^2.0.0",
    "gulp-typescript": "^2.3.0",
    "typescript": "^1.3.0",
    "vinyl-source-stream": "^1.0.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "gulp",
    "clean": "rm -fr {typings, dist, extension/bower_components, node_modules}",
    "fix": "fixpack && fixbow",
    "postinstall": "dtsm install && bower install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "gulp watch"
  }
}
